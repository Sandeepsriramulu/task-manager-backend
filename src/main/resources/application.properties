# ============================================================
# Task Manager Backend — Application Configuration
# ============================================================

# Server
server.port=8080

# Database (Local MySQL)
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/taskdb?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=taskapp
spring.datasource.password=TaskApp2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORS (allow frontend to call API)
# Configured in WebConfig.java
